# TestFlow Environment Variables
# Copy this file to .env (local development only)
# For production, set these in Render dashboard

# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=testflow
DB_PORT=3306
